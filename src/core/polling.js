/** @module core/polling */
/** @param {object} input @param {object} [options] @returns {number} */
export function computePollInterval(input, options) {
  throw new Error('not implemented');
}
export const ACTIVE_INTERVAL_MS = 0;
export const IDLE_INTERVAL_MS = 0;
export const LIVE_WINDOW_MS = 0;
export const AGENDA_RESERVE = 0;
