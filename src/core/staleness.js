/** @module core/staleness */
/** @param {number} timestampMs @param {number} nowMs @param {object} [options] @returns {import('./types.js').Staleness} */
export function isStale(timestampMs, nowMs, options) {
  throw new Error('not implemented');
}
export const FRESH_MAX_MS = 0;
export const WARN_MAX_MS = 0;
